export const metadata = {
  title: "Contact | Apanka Ayebadek David",
  description: "Have a project in mind, or just want to talk? Get in touch.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}